name: vocation-project
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - Flask==3.0.0
      - openai>=1.0.0
      - google-api-python-client>=2.0.0
      - google-auth>=2.0.0
      - google-auth-oauthlib>=1.0.0
      - gunicorn>=21.0.0
